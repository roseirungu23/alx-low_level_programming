#include "main.h"

/**
 * _isupper - checks if a letter is an uppercase
 * @x:Number to be checked
 * Return: 1 for upercase 0 otherwise
 */

int _isupper(int x)
{

	if (x >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
